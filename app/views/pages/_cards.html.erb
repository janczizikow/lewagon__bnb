<section class="cards">
  <div class="container">
    <h2><%# defined?(title).nil? ? 'Popular boats' : title %></h2>
    <div class="row">
      <div class="col-md-4">
        <%= render 'shared/card', rating: 4, card_title: "Hello" %>
      </div>
      <div class="col-md-4">
        <%= render 'shared/card', rating: 5  %>
      </div>
      <div class="col-md-4">
        <%= render 'shared/card', rating: 5  %>
      </div>
    </div>
  </div>
</section>
