<section class="boats__forms">
    <div class="container">
      <h1>Add new boat </h1>
      <%= react_component("Form", { action: '/boats', authenticity_token: form_authenticity_token }) %>
      <div class="small" style="margin: 16px 0;">
        <%= link_to "Go back to the boat list without saving", boats_path %>
      </div>
    </div>
  </section>
<%= content_for(:after_js) do %>
  <%= javascript_pack_tag 'server_rendering' %>
<% end %>
